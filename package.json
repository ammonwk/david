{
  "name": "david",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "dashboard",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:dashboard\"",
    "dev:server": "cd server && npm run dev",
    "dev:dashboard": "cd dashboard && npm run dev",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w dashboard",
    "test": "npm run test -w server && npm run test -w dashboard",
    "test:coverage": "npm run test:coverage -w server && npm run test:coverage -w dashboard",
    "test:server": "npm run test -w server",
    "test:dashboard": "npm run test -w dashboard"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
